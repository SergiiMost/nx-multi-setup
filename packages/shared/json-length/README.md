# json-length

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build json-length` to build the library.

## Running unit tests

Run `nx test json-length` to execute the unit tests via [Jest](https://jestjs.io).
